# .rigor.dist.yml — Rigor configuration (committed; shared).
# Generated by the rigor-project-init workflow.

# Project targets Ruby >= 3.2, but Prism supports Ruby 3.3 as its minimum.
# Setting target_ruby: "3.3" — syntax parsing is compatible across 3.0–3.3.
target_ruby: "3.3"

paths:
  - lib

pre_eval:
  - lib/binpacker/timing.rb
  - lib/binpacker/progress.rb

baseline: .rigor-baseline.yml
